# 디자인 — 종목 발굴

**정본은 플랜비 공통 가이드다.** `planb/docs/디자인_가이드__공통_.md` 를 그대로 따르고,
이 문서에는 **그 가이드가 다루지 않는 이 제품만의 규칙**만 적는다. 두 문서가 어긋나면 공통 가이드가 맞다.

- 골격·색·서체·여백·형태 → 공통 가이드 §1~§4
- 말투 → 공통 가이드 §5 (단, 아래 §3의 금융 관련 원칙을 겹쳐 본다)
- 안 하는 것 → 공통 가이드 §8

구현 파일: `discover_template.html` (스타일·화면), `src/discover.py` (전략 문구).
수정 후 `python src/build_discover.py` 로 `index.html` 을 다시 만든다.

---

## 1. 이 제품만의 토큰 — 시세 등락

공통 가이드에는 없는 축이다. 주식 화면은 오르내림을 색으로 구분해야 하고,
**한국 관행은 미국과 반대**다 (상승 = 빨강).

| 토큰 | 라이트 | 다크 | 쓰임 |
|---|---|---|---|
| `--up` | `#D70015` | `#FF6961` | 상승 (공통 `--danger` 와 같은 값) |
| `--down` | `#0B5FD0` | `#6FA8FF` | 하락 |

- 액센트(`#4659EF`, 인디고)와 하락(`#0B5FD0`, 파랑)은 채도·색상이 달라 구분된다.
  액센트는 링크·버튼에만, 하락은 숫자에만 나타나 자리가 겹치지 않는다.
- **등락 색은 숫자에만 칠한다.** 행 배경이나 카드 면을 칠하지 않는다 (공통 §3).
- 색만으로 전달하지 않는다 — 등락률에는 항상 `+` / `−` 부호가 붙는다.

## 2. 카테고리는 색으로 구분하지 않는다

일곱 개 카테고리에 각각 색을 줬다가 되돌렸다. 공통 가이드 §3 「뱃지·라벨은 무채색 글자」에
어긋나고, 일곱 색이 화면에 동시에 뜨면 산만하다.

- 선택 칩: 평소 `--surface` 채움, 선택 시 `--ink` 채움. **선택 상태는 채움 하나로만** (공통 §4)
- 1위 배지: `--accent` 하나. 카테고리별로 바꾸지 않는다
- 구분은 **이름과 순서**가 한다

## 3. 금융 화면의 말하기

공통 가이드 §5(해요체·구어체·강요 대신 제안)를 그대로 쓰되, 세무 금칙 대신 아래를 지킨다.

- **매매 신호를 낸다** (2026-09-05 방침 변경). 이전 원칙은 「매매를 권하지 않는다」였다.
  종목마다 매수·관망·청산 중 어느 구간인지와 목표가·손절선을 숫자로 말한다
- 대신 **네 가지를 반드시 같이 지킨다.** 하나라도 빠지면 신호를 내지 않는다
  1. **규칙을 화면에 그대로 편다.** 숨은 조건을 두지 않는다
  2. **그 규칙의 과거 성적을 같은 자리에 붙인다.** 이긴 달과 진 달을 모두 적는다
  3. **판정 문장도 규칙이 만든다.** 사람이 말을 고르지 않는다
     (`sigVerdict()` 한 군데서만 만들어 첫 화면과 상세 시트가 다른 말을 못 하게 한다)
  4. **가격만 본다.** 실적·뉴스·업황 해석을 섞지 않는다 — 섞는 순간 ①도 ②도 불가능해진다
- **개인 맞춤 조언은 하지 않는다.** 보유 종목·자금 사정·투자 성향에 맞춘 말은 하지 않는다.
  화면은 누가 보든 같은 문장을 보여준다. 투자자문업 등록 사업자가 아니다
- 성적이 나쁘면 나쁘다고 그대로 쓴다. 2026-09-05 실측에서 이 신호는
  미국은 지수와 거의 같았고(9승 15패) 한국은 지수보다 못했다(11승 12패). 둘 다 화면에 그대로 있다
- 「추천」은 탭 이름에만. 종목을 고른 이유는 **「골랐어요 / 걸러냈어요」**
- **형용사 대신 숫자** (공통 가이드와 동일). 「많이 올랐어요」가 아니라 「6개월 새 +167.6% 올랐어요」
- 전문용어를 화면에 쓰지 않는다

| 쓰지 않기 | 쓰기 |
|---|---|
| RSI 67.5 | 아직 과열 구간은 아니에요 |
| Fwd PER 6.1 (섹터 중앙값 13.2) | 같은 업종 평균보다 54% 싸요 |
| 순이익률 28% | 매출 100원당 28원이 이익으로 남아요 |
| 모멘텀 추세 | 잘 오르고 있는 주식 |
| 과매도 반등 후보 | 많이 떨어진 우량주 |

- 없는 값은 `—`. 정렬할 때 항상 뒤로 보낸다
- 면책 문구는 하단에 유지한다

## 4. 숫자 표시

- 자릿수가 붙는 곳은 전부 `.num` (`tabular-nums`) — 갱신될 때 글자가 흔들리지 않는다
- 큰 강조 수치는 **800 굵기**로 키운다 (공통 §6-3)
- 3개월 스파크라인은 **무채색**이다. 등락 색을 쓰면 「오늘 −4.5%인데 선은 빨강」 같은 모순이 생긴다

## 5. 로고

- `assets/logos/{티커}.png` — 128×128, 빌드 시점에 받아 저장소에 보관
- 방문자 브라우저가 외부 서버로 요청하지 않는다
- 없는 종목·로딩 실패는 두 글자 아바타 (`.lg.ph`)
- 크기: 1위 46 / 목록 28 / 보유 32 / 뉴스 16 / 표 22 (px)
- 배경은 `--surface` 회색 면. 로고에 테두리를 두르지 않는다

## 6. 갱신되는 화면이라서

숫자가 스스로 바뀌는 화면이다. 정적 페이지에는 없는 규칙이 필요하다.

- **장 상태를 항상 표시한다** — 장중 / 프리마켓 / 장 마감 / 주말 휴장
- 장이 닫혀 있으면 「몇 분 전」 대신 **다음 개장 시각**을 안내한다.
  마감 상태에서 「9시간 전」은 고장으로 읽힌다
- 첫 화면에 이미 보이는 요소는 **등장 효과 없이 즉시 표시**한다.
  관찰자에만 맡기면 투명하게 남는다 (실제로 겪었다)
- 빈 상태·로딩·에러 3종을 모두 만든다 (공통 §7). 흰 화면을 두지 않는다
